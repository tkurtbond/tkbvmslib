### ICONARGS.ICN - Display the arguments.
## Since Icon is written in C, the VAX C RTL converts the arguments to the
## program to lower case.
procedure main (args)
  while arg := get (args) do
    write (arg)
end # main
